using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BathwaterSurface : MonoBehaviour
{
	// This component is for the mesh collider representing the surface of the water, for accurately playing effects.

	void Start()
	{

	}

	// Update is called once per frame
	/* void OnTriggerStay(Collider col)
	{
		if (col.TryGetComponent<FloaterController>(out FloaterController fc))
		{
			fc.skimming = true;
		}
	} */
}
